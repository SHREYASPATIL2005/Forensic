import 'package:flutter/material.dart';

void main() {
  runApp(CarApp());
}

class CarApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Padding(
          padding: EdgeInsets.all(20),
          child: Column(
            children: [

              // Sedan Container
              Container(
                width: double.infinity,
                padding: EdgeInsets.all(15),
                margin: EdgeInsets.only(bottom: 10),
                color: Colors.blue,
                child: Text(
                  "Sedan",
                  style: TextStyle(
                    color: Colors.white,
                    fontSize: 18,
                  ),
                ),
              ),

              Text("Sedans are comfortable cars for families."),

              SizedBox(height: 20),

              // SUV Container
              Container(
                width: double.infinity,
                padding: EdgeInsets.all(15),
                margin: EdgeInsets.only(bottom: 10),
                color: Colors.green,
                child: Text(
                  "SUV",
                  style: TextStyle(
                    color: Colors.white,
                    fontSize: 18,
                  ),
                ),
              ),

              Text("SUVs are powerful and suitable for long trips."),

              SizedBox(height: 20),

              // Hatchback Container
              Container(
                width: double.infinity,
                padding: EdgeInsets.all(15),
                margin: EdgeInsets.only(bottom: 10),
                color: Colors.orange,
                child: Text(
                  "Hatchback",
                  style: TextStyle(
                    color: Colors.white,
                    fontSize: 18,
                  ),
                ),
              ),

              Text("Hatchbacks are small and easy to drive."),
            ],
          ),
        ),
      ),
    );
  }
}
